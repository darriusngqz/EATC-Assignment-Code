streamlit==1.59.1
scikit-learn==1.9.0
# Pinned below 3.0: xgboost 3.x changed how it stores base_score internally
# (wraps it as "[0.5]" instead of "0.5"), which shap's TreeExplainer cannot
# parse yet, breaking SHAP explainability for the deployed model entirely.
# Verified 2.0.3 + shap 0.49.1 work together in 06_evaluation.ipynb.
xgboost==2.0.3
shap==0.49.1
pefile==2024.8.26
pandas==3.0.3
numpy==2.4.6
joblib==1.5.3
