# Windows PE malware classifier

EATC Assignment 2 project code: a static PE-header malware classifier
(Random Forest / XGBoost, deployed as a Streamlit app) with a Multi-Layer
Perceptron trained alongside it purely for comparison (not deployed).

The full modelling workflow lives in `notebooks/`, one notebook per stage of the
data science lifecycle. For the full rationale behind each design decision
(leakage-safe pipelines, the shortcut-learning check, the security boundary on
file uploads), read the notebooks themselves, they explain each step as they go.

## Dataset (already included, no download needed)

- `data/dataset_malwares.csv` - 19,611 labelled rows, 77 numeric PE-header
  features + `Name` + `Malware` (1 = malicious, 0 = benign). Malicious rows
  are named by VirusShare hash; benign rows are named real Windows/software
  files. Used to train and evaluate the models.
- `data/dataset_test.csv` - 17 real files (Skype, VLC, Spotify, WinRAR,
  some SHA-256-named samples, `reverse_shell.exe`, etc.), same 77 feature
  columns, **no label column**. Used by the app's "Try a sample" tab for
  live demos; treat any verdict on these as a discussion point, not a
  graded/verified answer, since no ground truth is provided.
- `data/dataset_cleaned.csv` - generated by `notebooks/02_data_preparation.ipynb`
  (all 77 features, duplicate rows dropped). Used by `notebooks/03_eda.ipynb`
  for visual exploration only.

`04_modelling_classical.ipynb`, `05_modelling_mlp.ipynb`, and
`06_evaluation.ipynb` all train directly on `dataset_malwares.csv`, the
original published dataset, no rows added or removed. An earlier version of
this project added real legitimate files to the training data specifically to
reduce a false-positive rate found during real-world testing; that file
(`dataset_augmented.csv`) has been removed, since changing training data to
fix a weakness found through evaluation is not sound data science practice.
See Section 5 below for how the resulting real-world false-positive rate is
now handled instead: measured and reported honestly, not engineered away.

`src/constants.py` (`ORDER_OF_FEATURES`) is the single source of truth for
the 77 feature names and their order; every notebook and the app import it, so
they can never silently drift apart.

## 1. Setup

```bash
python -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate

pip install -r requirements-dev.txt   # everything, for the notebooks + testing
# or, once you only need to run the deployed app:
pip install -r requirements.txt
```

`requirements.txt` pins `xgboost==2.0.3` and `shap==0.49.1` together on
purpose: newer XGBoost (3.x) changed how it stores `base_score` internally in
a way SHAP's `TreeExplainer` cannot parse yet, which silently breaks the
explainability feature. This was discovered and fixed while building
`notebooks/06_evaluation.ipynb`, see that notebook's Summary for details.

## 2. Run the notebooks (in order)

```bash
jupyter notebook notebooks/
```

Run each one top to bottom (Restart & Run All), in this order:

| Stage | Notebook | What it does |
|-------|----------|---------------|
| Data Understanding | [`01_data_understanding.ipynb`](notebooks/01_data_understanding.ipynb) | First uncleaned look at the raw dataset: shape, class balance, missing values, duplicates, the zero-variance feature discovery, and the DLL-vs-EXE composition |
| Data Preparation | [`02_data_preparation.ipynb`](notebooks/02_data_preparation.ipynb) | Cleans the data (drops missing/duplicate rows), saves `data/dataset_cleaned.csv` |
| EDA | [`03_eda.ipynb`](notebooks/03_eda.ipynb) | Visualises the cleaned data and runs the bias diagnostic that proves the DLL-vs-EXE shortcut is not the only thing the model learns |
| Modelling | [`04_modelling_classical.ipynb`](notebooks/04_modelling_classical.ipynb) | Tunes and validates Random Forest and XGBoost on `CORE_TRAITS` (10 behavioural features), trained on the original `dataset_malwares.csv` |
| Modelling | [`05_modelling_mlp.ipynb`](notebooks/05_modelling_mlp.ipynb) | Trains and validates the MLP deep-learning comparison model, same data and split as `04` |
| Evaluation | [`06_evaluation.ipynb`](notebooks/06_evaluation.ipynb) | Compares all three `CORE_TRAITS` candidates (all trained on `dataset_malwares.csv`) on the same split, selects a winner, runs the one-time test-set check, explains predictions with SHAP, and saves `models/classical_pipeline.joblib` |
| Real-World Validation | [`07_real_world_validation.ipynb`](notebooks/07_real_world_validation.ipynb) | Needs real `.exe`/`.dll` files on disk: scans a folder assumed all-legitimate for false positives, scores known-malicious/known-benign folders for real accuracy, and explains any false positives with SHAP. Reports whatever false-positive rate is found honestly, as a dataset shift finding, not something to fix by reshaping training data; run this after `06` to validate the deployed model against real files, not just the held-out CSV split |

Only `constants.py`, `extract_features.py`, and `explain.py` remain as
importable `.py` modules in `src/`, since training and the deployed app must
always compute features and explanations the exact same way (see
`extract_features.py`'s own module docstring). Every other step (cleaning,
splitting, tuning, evaluating, augmenting) lives in the notebooks themselves.

## 3. Run the app locally

```bash
streamlit run app.py
```

Opens at `http://localhost:8501`. Requires `models/classical_pipeline.joblib`
(produced by `06_evaluation.ipynb`). Three tabs: upload a real `.exe`/`.dll`,
try a real file from `data/dataset_test.csv`, or batch-classify a CSV of
already-extracted feature rows. The malicious/benign cutoff is the standard
0.5, see `app.py`'s `MALICIOUS_THRESHOLD` comment. This model trains only on
the original, unaugmented `dataset_malwares.csv`; its real-world
false-positive rate against `C:\Windows\System32` is measured and reported
honestly in Section 5 below, not adjusted by changing training data or the
decision threshold to hit a target number.

**NSRL hash verification and VirusTotal (Upload tab only).** The model's own
verdict is always the primary result, these two checks exist to help spot a
false positive or false negative, not to replace the model's judgement.

After a file is uploaded, its SHA-256 hash is checked against CIRCL's
`hashlookup.circl.lu`, a free public API run by Luxembourg's national CERT
that queries NIST's National Software Reference Library (NSRL) live. CIRCL
also aggregates known-malware hash lists (e.g. malshare.com) in the same
lookup, so a match is read two ways: a genuine NSRL record overrides the
verdict to benign, a `KnownMalicious` record overrides it to malicious. A
match to neither, no match at all, or the service being unreachable, all
fall through to the model's own verdict unchanged. The model's own
prediction is always shown alongside for transparency, even when overridden.

VirusTotal is checked separately and never overrides anything, purely
informational: it shows the multi-engine detection ratio (e.g. "2/71 engines
flag this as malicious") next to the verdict. If it disagrees with the final
verdict, a note explains this is an expected limitation of static-only
analysis, not proof either result is wrong, and prompts a closer look rather
than auto-correcting. This design is deliberate: malware authors routinely
test samples against VirusTotal and iterate until detection is low, so
treating a low detection count as proof of safety would mean trusting
exactly the signal attackers optimise around, defeating the purpose of a
model built to catch what signature-based antivirus misses. VirusTotal is
optional, it only runs if `VIRUSTOTAL_API_KEY` is set in
`.streamlit/secrets.toml` (see `.streamlit/secrets.toml.example`), get a
free key yourself at virustotal.com/gui/join-us, silently skipped otherwise.

Both checks require internet access at runtime and are separate, independent
detection layers, neither touches the training data, features, or model in
any way. Both only apply to the Upload tab: the "Try a sample" and "Batch
CSV" tabs work from already-extracted numeric features, there is no original
file left to hash. See `app.py`'s `check_nsrl_hash` and `check_virustotal`
for the implementation.

## 4. Run the tests

```bash
pytest tests/ -v
```

`tests/test_extract_features.py` runs without `pefile` or a real PE file
(tests the feature-computation logic against small fake objects).
`tests/test_pipeline.py` needs `scikit-learn` and trains a tiny throwaway
pipeline on synthetic data in under a second; it does not touch the real
dataset or the production model file. Both only depend on `constants.py` and
`extract_features.py`, so they are unaffected by anything the notebooks do.

## 5. Validate against real files and report the result honestly (run locally, in `07_real_world_validation.ipynb`)

`07_real_world_validation.ipynb` documents, but cannot execute in every
environment, two checks that need real `.exe`/`.dll` files on disk (each a
plain sequence of cells, not a wrapper function):

- **Scan a folder assumed all-legitimate** (e.g. `C:\Windows\System32`) and
  report the false-positive rate.
- **Explain any false positives with SHAP**, reading each one against
  per-feature training averages.

A third check, scoring known-malicious and known-benign folders for real
accuracy/precision/recall, was deliberately left out: it needs real malware
samples handled in a properly isolated environment, which this project does
not have safe access to, using placeholder or improvised "malicious" files
for that number would be worse than not having it.

Real local testing (200 files under `C:\Windows\System32`) found the model
trained on `CORE_TRAITS`, on the original, unaugmented `dataset_malwares.csv`,
flagged 90.5% of genuine `C:\Windows\System32` files as malicious, far too high
to be usable in
practice. An earlier version of this project responded to that finding by
adding real legitimate files into the training data (`augment_with_real_benign_files()`,
producing a file called `dataset_augmented.csv`), which did bring the
false-positive rate down to 8.5-9.5%. That approach, and the file it produced,
have both been removed from this project. Changing training data specifically
to fix a weakness discovered through evaluation is not sound data science
practice, it risks a model tuned to pass one specific test rather than one
that genuinely generalises, and was flagged as such by course guidance on
this assignment. Synthetic oversampling (e.g. SMOTE) was also considered and
rejected for the same reason when used reactively for this purpose: it can
only recombine information already present in the training data, so it
cannot manufacture examples of a type of legitimate file the dataset never
had (typical Windows system files), and using it specifically to chase this
number would share the same underlying problem.

The correct way to understand this result is as **dataset shift**
(also called distribution shift): the training data's benign class does not
structurally resemble the files this model is tested against in deployment.
This is a well-documented, common failure mode in real-world machine
learning, not a defect unique to this pipeline. The honest response is to
report the real-world false-positive rate exactly as measured, alongside the
held-out validation numbers, and to treat it as a genuine limitation with a
properly scoped recommendation for future work: a dedicated, transparent data
collection phase to build a training set that actually represents typical
deployed software, carried out as its own deliberate project phase rather
than a reactive patch. See `07_real_world_validation.ipynb`'s Summary and the
project report's limitations section for the full discussion.

As a genuinely different kind of mitigation, not a substitute for the future
work above, `app.py`'s Upload tab also checks an uploaded file's hash against
NIST's NSRL via the CIRCL `hashlookup` API, and optionally shows a
VirusTotal detection ratio for context (see Section 3). This is
architecturally distinct from changing the training data or model: it adds
independent, external, industry-standard detection layers on top of an
honestly-evaluated model, the same way real antivirus/EDR products combine
static analysis with signature/hash matching and multi-engine reputation as
separate layers, rather than tuning any one of them to agree with the
others. It does not fix the underlying dataset shift, and only ever applies
to files that are actually uploaded (not the pre-extracted `dataset_test.csv`
rows), but it reduces false positives (and catches confirmed malware) on
files an independent source already has an opinion on. VirusTotal
specifically never overrides the model, both because a low detection count
isn't proof of safety, and because malware authors test against VirusTotal
specifically to find variants with low detection, so trusting that signal
blindly would undermine a model built to catch what signature-based
antivirus misses.

Real-world testing during development, using files actually downloaded
locally rather than the training/test CSVs, found NSRL's practical hit rate
lower than expected: a Windows System32 DLL, a Spotify installer, and a
7-Zip installer all returned no match. This is expected, not a defect, NSRL
only contains hashes someone specifically submitted, and frequently-updated
or vendor-unsubmitted software often isn't covered, but it is worth stating
honestly here rather than overselling the check's real-world coverage.

## 6. Deploy to Streamlit Community Cloud

1. Push this repository to GitHub, with `app.py` in the repository root and
   `models/classical_pipeline.joblib` committed (or generated by a setup
   step, e.g. a GitHub Action that runs the notebooks on deploy).
2. Go to share.streamlit.io, sign in, "Create app", point it at this repo,
   branch, and `app.py`.
3. `requirements.txt` (not `requirements-dev.txt`) is what Streamlit Cloud
   installs, keep `tensorflow`/`pytest`/`matplotlib` out of it.
4. Manually re-test all three tabs against the live URL once deployed.

## Project layout

```
malware-pe-classifier/
  data/
    dataset_malwares.csv         # training data, 19,611 labelled rows; what 04/05/06 actually train on, unmodified
    dataset_test.csv             # 17 real, unlabelled demo files
    dataset_cleaned.csv          # all-features cleaned copy (02_data_preparation.ipynb, generated)
  models/                        # saved model artifacts (created by the notebooks)
  notebooks/
    01_data_understanding.ipynb
    02_data_preparation.ipynb
    03_eda.ipynb
    04_modelling_classical.ipynb
    05_modelling_mlp.ipynb
    06_evaluation.ipynb
    07_real_world_validation.ipynb   # needs real .exe/.dll files on disk; reports the real-world result honestly
  src/
    constants.py                 # ORDER_OF_FEATURES, feature-set groupings (all/behavioral/core/raw), DROPPED_FEATURES
    extract_features.py          # PE parsing -> feature row (shared by every notebook + the app)
    explain.py                   # SHAP explainability (used by 06_evaluation.ipynb and app.py)
  tests/
    test_extract_features.py
    test_pipeline.py
  app.py                         # Streamlit app (deployed)
  requirements.txt               # deployed app dependencies only
  requirements-dev.txt           # + notebook/testing dependencies
```

## What has and hasn't been run

All 14 tests in `tests/` pass.

`01_data_understanding.ipynb` and `02_data_preparation.ipynb` have been run
end to end and their Summary sections match their own cell output exactly:
19,611 raw rows, 0 missing values, 2,984 duplicate-feature rows, 8
zero-variance columns (7 a confirmed dataset bug plus the expected-constant
`e_magic`), 16,627 rows after cleaning, plus a `describe()` statistical
summary of the 10 `CORE_TRAITS` features showing every one is heavily
right-skewed. `03_eda.ipynb` has also been run and is internally consistent
(84.2% of benign rows are DLLs vs 8.8% of malicious rows, a `CORE_TRAITS`
correlation check finding one redundant pair, `SectionMinRawsize` and
`SectionMinVirtualsize` at r = 1.00, and a bias diagnostic trained on all 77
features scores 95.08% on the deliberately "hard" subset, far above the 50% a
shortcut-only model would reach, confirming the model learns genuine signal
beyond file type).

`04_modelling_classical.ipynb`, `05_modelling_mlp.ipynb`, and
`06_evaluation.ipynb` all now train on the same, original, unaugmented
`dataset_malwares.csv`, no rows added or removed. This project previously
trained on an augmented dataset (real legitimate files added to reduce a
real-world false-positive rate found in testing); that dataset and the
augmentation step that built it have both been removed, since changing
training data to fix a weakness found through evaluation is not sound
practice (see Section 5 above for the full reasoning). `04`'s `load_and_clean`
cell has real, executed output on the current data source (19,611 rows
loaded, 6,094 duplicate-feature rows dropped, 13,517 remain, 64.25% malicious
/ 35.76% benign), but the cells after it, and all of `05` and `06`'s
model-training cells, need a fresh **Restart & Run All** on a machine with
`scikit-learn`/`xgboost`/`tensorflow` installed before their accuracy, F1,
and ROC-AUC numbers, and the model files they save
(`models/mlp_comparison.keras`, `models/classical_pipeline.joblib`), can be
trusted. Each notebook's Summary cell has a `TODO` marking exactly what to
fill in after that re-run.

Confirmed via a real local run of `07_real_world_validation.ipynb`'s Check 1
(200 files scanned under `C:\Windows\System32`, 0 unparseable): this
unaugmented `CORE_TRAITS` model flagged 90.5% (181/200) of genuine files as
malicious, at the standard 0.5 decision threshold `app.py` uses. This is
reported here honestly as a dataset shift finding (the training data's
benign class does not structurally resemble typical Windows system files),
not as something this project tries to fix by reshaping the training data
further, per Section 5's reasoning. If `04`-`06` are re-run and the model
changes, re-run the real-world scan again and report whatever false-positive
rate is found honestly alongside the validation-set numbers, not instead of
them.

The 24 "derived" features (`SuspiciousImportFunctions`,
`SuspiciousNameSection`, the `Section*` min/max stats, `SectionMainChar`,
`DirectoryEntry*`, `ImageDirectoryEntry*`) follow the published ClaMP
dataset's design philosophy (github.com/urwithajit9/ClaMP) since the exact
private script that built `dataset_malwares.csv` isn't published. Their
definitions are documented inline in `src/extract_features.py`. Before
relying on this for grading, extract features from a few files you can
also look up in `dataset_malwares.csv` and sanity-check the values are
plausible.
